---
import themes from '../data/themes.json';
import publications from '../data/publications.json';
const byId = Object.fromEntries(publications.publications.map(p => [p.id, p]));
---
<nav class="map" id="map" aria-label="Page map">
  <div class="map-items" id="map-items">
    {themes.map((th, ti) => th.papers.map((id, pi) => (
      <a class={`map-item${pi === 0 && ti > 0 ? ' gap' : ''}`} data-th={th.id} href={`#w-${id}`} data-map={`w-${id}`} data-top={pi === 0 ? `t-${th.id}` : undefined}><span>{byId[id].shortName}</span></a>
    )))}
    <a class="map-item is-pubs gap" href="#publications" data-map="publications"><span>All publications</span></a>
  </div>
  <div class="map-view" id="map-view" aria-hidden="true"></div>
</nav>
